package taskManager;


//basically enters the task in the schedulerQueue and spits it out once the time is reached and moves the task to the executor

//instance that would eventually create/need/have the timeprovider, the scheduler queue, the executor thing.
public class TaskManager {
    
}
